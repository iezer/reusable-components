import { submitButton } from '../../../helpers/submit-button';
import { module, test } from 'qunit';

module('Unit | Helper | submit button');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = submitButton(42);
  assert.ok(result);
});
